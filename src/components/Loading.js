import React, { Component } from 'react';

class Loading extends Component {
  render() {
    return (
      <div className="text-red-fireEngine">Carregando...</div>
    );
  }
}

export default Loading;
