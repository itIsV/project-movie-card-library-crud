import React, { Component } from 'react';

class Loading extends Component {
  render() {
    return (
      <div className="text-red-fireEngine text-4xl">Carregando...</div>
    );
  }
}

export default Loading;
